# diff-report

## Abstract goal

Turn git history diffs into shareable HTML/PDF reports.

## Dependencies

- `bash`
- `git`
- `sed`
- `mktemp`
- `delta` (for colorized diff rendering)
- `aha` (for ANSI-to-HTML conversion)
- `wkhtmltopdf` (for PDF output)
